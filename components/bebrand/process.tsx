import { process } from '@/lib/bebrand-content';
export function Process() { return <section className="process-section" id="proceso"><div className="container"><div className="section-heading"><div><span className="eyebrow">ASÍ TRABAJAMOS</span><h2>Contigo.<br/><em>En cada etapa.</em></h2></div><p>Un proceso claro, comunicación directa<br/>y decisiones que tomamos juntos.</p></div><ol className="process-grid">{process.map(step=><li key={step.number}><span className="step-number">{step.number}</span><h3>{step.title}</h3><p>{step.description}</p></li>)}</ol></div></section> }
