import { Globe2, ShoppingBag, Code2, Workflow } from 'lucide-react';
import { services } from '@/lib/bebrand-content';
const icons = { web: Globe2, shop: ShoppingBag, code: Code2, workflow: Workflow };
export function Services() { return <section className="services-section container" id="servicios"><div className="section-heading"><div><span className="eyebrow">LO QUE HACEMOS</span><h2>De una buena idea<br/><em>a algo que funciona.</em></h2></div><p>La tecnología adecuada.<br/>Para lo que tu negocio necesita.</p></div><div className="service-grid">{services.map(service=>{const Icon=icons[service.icon as keyof typeof icons];return <article className="service-card" key={service.number}><div className="service-top"><span>{service.number}</span><Icon size={26} strokeWidth={1.25}/></div><h3>{service.title}</h3><p className="service-subtitle">{service.subtitle}</p><p className="service-description">{service.description}</p><div className="service-tags">{service.tags.map(tag=><span key={tag}>{tag}</span>)}</div></article>})}</div></section> }
